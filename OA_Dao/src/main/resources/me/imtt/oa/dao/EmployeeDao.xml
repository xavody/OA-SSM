<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.4//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.imtt.oa.dao.EmployeeDao">
    <resultMap id="employeeMap" type="Employee">
        <id property="sn" column="sn" javaType="String"/>
        <result property="password" column="password" javaType="String"/>
        <result property="name" column="name" javaType="String"/>
        <result property="departmentSn" column="department_sn" javaType="String"/>
        <result property="post" column="post" javaType="String"/>
        <result property="authId" column="auth_id" javaType="Integer"/>
        <association property="department" column="department_sn" javaType="Department">
            <id property="sn" column="dsn" javaType="String"/>
            <result property="name" column="dname" javaType="String"/>
        </association>
        <association property="authority" column="auth_id" javaType="Authority">
            <result property="level" column="level" javaType="Integer"/>
        </association>
    </resultMap>

    <insert id="insert" parameterType="Employee">
        insert into employee
        values (#{sn}, #{password}, #{name}, #{departmentSn}, #{post})
    </insert>

    <update id="update" parameterType="Employee">
        update employee
        set name          = #{name},
            password      = #{password},
            department_sn = #{departmentSn},
            post          = #{post}
        where sn = #{sn}
    </update>

    <delete id="delete" parameterType="String">
        delete
        from employee
        where sn = #{sn}
    </delete>

    <select id="select" parameterType="String" resultMap="employeeMap">
        select e.*, d.sn dsn, d.name dname, a.level
        from employee e
                 left join department d on d.sn = e.department_sn
                 left join authority a on a.id = e.auth_id
        where e.sn = #{sn}
    </select>

    <select id="selectAll" resultMap="employeeMap">
        select e.*, d.sn dsn, d.name dname, a.level
        from employee e
                 left join department d on d.sn = e.department_sn
                 left join authority a on a.id = e.auth_id
    </select>

    <select id="selectByDepartmentAndPost" resultMap="employeeMap">
        select e.*, d.sn dsn, d.name dname
        from employee e
        left join department d on d.sn = e.department_sn
        left join authority a on a.id = e.auth_id
        where e.sn is not null
        <if test="dsn!=null">
            and e.department_sn = #{dsn}
        </if>
        <if test="post!=null">
            and e.post = #{post}
        </if>
    </select>
</mapper>